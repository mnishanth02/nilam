{
  "name": "nilam",
  "private": true,
  "packageManager": "pnpm@10.30.1",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "dev": "turbo dev",
    "dev:web": "turbo dev --filter=@nilam/web",
    "build": "turbo build",
    "lint": "turbo lint",
    "format": "pnpm exec biome format --write .",
    "format:check": "pnpm exec biome format .",
    "check": "pnpm exec biome check --write .",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "test:unit": "vitest run --config vitest.workspace.ts",
    "test:watch": "vitest --config vitest.workspace.ts",
    "test:web": "pnpm --filter @nilam/web test:e2e",
    "update": "pnpm up -r --latest",
    "db:generate": "pnpm --filter @nilam/db db:generate",
    "db:migrate": "pnpm --filter @nilam/db db:migrate",
    "db:push": "pnpm --filter @nilam/db db:push",
    "db:studio": "pnpm --filter @nilam/db db:studio",
    "db:seed": "pnpm --filter @nilam/db db:seed",
    "db:reset": "pnpm --filter @nilam/db db:reset",
    "db:cleanup": "pnpm --filter @nilam/db db:cleanup",
    "clean": "rm -rf node_modules .turbo apps/*/node_modules apps/*/.next apps/*/.expo packages/*/node_modules packages/*/dist && find . -name '*.tsbuildinfo' -delete"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.9",
    "turbo": "2.8.20",
    "vitest": "^3.2.4"
  }
}
