import { Hono } from 'hono';

const app = new Hono();

// TODO: Implement lease routes
// Key endpoints:
// GET /
// POST /
// GET /:id
// PUT /:id
// POST /:id/activate
// POST /:id/terminate

export default app;
